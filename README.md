# The Lakes Barbershop — Static Site

Flat-file website. No framework, package manager, build step, or runtime required.

Files:
- `index.html` — complete page markup
- `style.css` — site styling and responsive rules
- `images/` — original image assets
- `og.png` — social preview image

Open `index.html` directly or serve this folder from any static web host.
